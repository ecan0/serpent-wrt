## Summary

What changed?

## Validation

- [ ] `go test ./...`
- [ ] `go vet ./...`
- [ ] `make release-check` for release/package/process changes
- [ ] OpenWrt runtime/package test, if applicable

## Notes

Mention OpenWrt target impact, config changes, or release/package metadata
changes here.
For runtime validation, describe the target class and CI status without private
hostnames, IP addresses, or SSH key paths.
